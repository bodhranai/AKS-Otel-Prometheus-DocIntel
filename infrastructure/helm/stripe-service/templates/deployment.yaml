apiVersion: apps/v1
kind: Deployment
metadata:
  name: stripe-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: stripe-service
  template:
    metadata:
      labels:
        app: stripe-service
    spec:
      containers:
        - name: stripe-service
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 80
